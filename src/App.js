import React from "react";
import Home from "./components/Home";
import { Alert, Container } from "react-bootstrap";

const App = (props) => {
  return (
    <Container>
      <Home></Home>
    </Container>
  );
};
export default App;
